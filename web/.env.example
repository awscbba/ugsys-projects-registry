# Projects Registry API (this service)
VITE_API_BASE_URL=http://localhost:8000

# Identity Manager API — auth endpoints (login, register, refresh, password)
# In prod: https://auth.apps.cloud.org.bo
# In local dev: leave empty to fall back to VITE_API_BASE_URL (proxy handles /api/v1)
VITE_AUTH_API_URL=http://localhost:8001
